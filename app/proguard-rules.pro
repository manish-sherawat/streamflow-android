# Add project-specific ProGuard rules here.
# ExoPlayer/Media3, Hilt, and Firebase all ship consumer-rules.pro bundled in
# their AARs, so no manual keep rules are required for the MVP dependency set.
# Add rules here if you introduce reflection-heavy libraries later (e.g. Gson
# model classes that get renamed by R8 and break JSON parsing).
