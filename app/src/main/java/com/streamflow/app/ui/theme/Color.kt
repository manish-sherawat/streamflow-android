package com.streamflow.app.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds (Ultra-Dark Premium Void)
val BgBase = Color(0xFF080B11)
val BgElevated = Color(0xFF121722)
val BgGradientTop = Color(0xFF1A2130)

// Glassmorphism
val GlassFill = Color(0x1AFFFFFF)      // 10% frosted fill
val GlassBorder = Color(0x33FFFFFF)    // 20% refined border
val GlassHighlight = Color(0x40FFFFFF) // 25% edge glow

// Text Typography Colors
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFA0AEC0)
val TextTertiary = Color(0xFF718096)

// Accents & Gradients (Neon Electric Indigo & Cyan Glow)
val AccentPrimary = Color(0xFF00E5FF)
val AccentSecondary = Color(0xFF7C4DFF)
val AccentMagenta = Color(0xFFFF2A6D)
val AccentFocus = Color(0xFF00B0FF)

// Metadata Badges & Pills
val PillBg = Color(0x26FFFFFF)
val PillBorder = Color(0x40FFFFFF)

// Divider & Lines
val Divider = Color(0x1EFFFFFF)
